// Function to log user action
function logUserAction(userAction) {
    console.log(`User performed: ${userAction}`);
}

// Example usage
logUserAction("logged in");
logUserAction("viewed dashboard");
logUserAction("logged out");
logUserAction("Item to AddCart")
